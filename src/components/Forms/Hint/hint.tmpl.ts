export const tmpl = `
  <div class="form-hint">
    <p class="hint-text">{{hintText}}</p>
    <a class="hint-link-block" href={{to}} name="{{name}}"}}>
      <p>{{hintLinkText}}</p>
      <div class="hint-icon"></div>
    </a>
  </div>
`;
